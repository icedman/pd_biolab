{
  char *type_name = "respawn_pod";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(576, 80);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "respawn_pod";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1668, 112);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "respawn_pod";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1216, 24);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1831, 99);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(503, 123);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1548, 68);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "debris";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(88, 48);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "trigger";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1720, 140);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1347, 83);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1268, 116);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "spike";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1964, 95);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "trigger";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(128, 80);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(403, 139);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "mine";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1440, 35);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "mine";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1528, 147);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "mine";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1576, 147);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "spike";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1132, 31);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1363, 139);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1555, 43);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "spike";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(816, 103);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "debris";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(152, 32);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "trigger";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1912, 140);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "player";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(76, 122);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "hurt";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1828, 68);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "trigger";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1848, 140);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(408, 100);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "trigger";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(2032, 80);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "trigger";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1784, 140);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "level_change";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(2052, 88);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1892, 92);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "spike";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1072, 95);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(816, 124);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1164, 84);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "spike";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1332, 39);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "blob";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(655, 83);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(1764, 108);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "test_tube";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(356, 100);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "delay";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(280, 44);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "earthquake";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(176, 96);
  entity_t *ent = entity_spawn(type, pos);
}

{
  char *type_name = "debris";
  entity_type_t type = entity_type_by_name(type_name);
  vec2_t pos = vec2(208, 8);
  entity_t *ent = entity_spawn(type, pos);
}

{
  map_t *map = malloc(sizeof(map_t));
  memset(map, 0, sizeof(map_t));
  map->size.x = 30;
  map->size.y = 20;
  map->tile_size = 8;
  map->distance = 2;
  char *name = "background";
  strcpy(map->name, name);
  if (str_equals(name, "collision"))
    engine_set_collision_map(map);
  else
    engine_add_background_map(map);
  map->tileset = image("assets/tiles/biolab.qoi");
  const int dataSize = sizeof(uint16_t) * map->size.x * map->size.y;
  map->data = malloc(dataSize);
  int index = 0;
  for (int y = 0; y < map->size.y; y++) {
    for (int x = 0; x < map->size.x; x++, index++) {
      map->max_tile = max(map->max_tile, map->data[index]);
      map->data[index] = data1[index];
    }
  }
}

{
  map_t *map = malloc(sizeof(map_t));
  memset(map, 0, sizeof(map_t));
  map->size.x = 256;
  map->size.y = 20;
  map->tile_size = 8;
  map->distance = 1;
  char *name = "collision";
  strcpy(map->name, name);
  if (str_equals(name, "collision"))
    engine_set_collision_map(map);
  else
    engine_add_background_map(map);
  const int dataSize = sizeof(uint16_t) * map->size.x * map->size.y;
  map->data = malloc(dataSize);
  int index = 0;
  for (int y = 0; y < map->size.y; y++) {
    for (int x = 0; x < map->size.x; x++, index++) {
      map->max_tile = max(map->max_tile, map->data[index]);
      map->data[index] = data2[index];
    }
  }
}

{
  map_t *map = malloc(sizeof(map_t));
  memset(map, 0, sizeof(map_t));
  map->size.x = 256;
  map->size.y = 20;
  map->tile_size = 8;
  map->distance = 1;
  char *name = "main";
  strcpy(map->name, name);
  if (str_equals(name, "collision"))
    engine_set_collision_map(map);
  else
    engine_add_background_map(map);
  map->tileset = image("assets/tiles/biolab.qoi");
  const int dataSize = sizeof(uint16_t) * map->size.x * map->size.y;
  map->data = malloc(dataSize);
  int index = 0;
  for (int y = 0; y < map->size.y; y++) {
    for (int x = 0; x < map->size.x; x++, index++) {
      map->max_tile = max(map->max_tile, map->data[index]);
      map->data[index] = data3[index];
    }
  }
}
